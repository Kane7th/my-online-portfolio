MySmartRental 2.0 staging CORS ù quick pointer
==============================================

FULL INSTRUCTIONS FOR VPS CURSOR AGENT:
  vps-config/mysmartrental/VPS-AGENT-RUNBOOK.md
  (discovery, correct app vs v1, nginx/Next steps, verification)

TARGET (only this):
  https://staging.mysmartrental.com/api/health
  Next.js MySmartRental 2.0 ù NOT https://mysmartrental.com (Flask v1)

Portfolio origin to allow:
  https://kane7th.github.io

Fast path (nginx) ó DONE on VPS May 2026:
  1. Edit /etc/nginx/sites-available/staging.mysmartrental.com only
  2. location = /api/health ? proxy_pass http://127.0.0.1:8080 (Docker staging, NOT :3000 prod)
  3. sudo nginx -t && sudo systemctl reload nginx

Staging stack: /opt/mysmartrental (Docker Compose). PM2 projectchateau = production only.

Verify:
  curl -sSI "https://staging.mysmartrental.com/api/health" -H "Origin: https://kane7th.github.io"
  ? must include: Access-Control-Allow-Origin: https://kane7th.github.io

Browser:
  https://kane7th.github.io/my-online-portfolio/
  ? MySmartRental 2.0 card shows "Staging: API + DB online" or "Staging: API online"
